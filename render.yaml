services:
  - type: web
    name: opticrop
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 4 -b 0.0.0.0:$PORT "5.Project_Development_Phase.Application.app:app"
    plan: free