services:
  - type: web
    name: opticrop
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: |
      cd /opt/render/project/src/5.Project_Development_Phase
      gunicorn -w 4 -b 0.0.0.0:$PORT Application.app:app
    plan: free
